{
  "name": "rh",
  "version": "1.0.0",
  "description": "RH module - clean Node+Express architecture",
  "main": "src/server.js",
  "scripts": {
    "styles:build": "sass src/public/styles/scss/app.scss src/public/styles/style.css --no-source-map",
    "styles:watch": "sass --watch src/public/styles/scss/app.scss:src/public/styles/style.css --no-source-map",
    "start": "npm run styles:build && node src/server.js",
    "dev": "npm run styles:build && nodemon src/server.js",
    "test": "jest --runInBand --passWithNoTests",
    "pm2:start": "pm2 start src/server.js --name pdms-rh",
    "pm2:start:watch": "pm2 start src/server.js --name pdms-rh --watch --ignore-watch=\"node_modules .git logs\"",
    "pm2:restart": "pm2 restart pdms-rh",
    "pm2:restart:watch": "pm2 restart pdms-rh --watch",
    "pm2:restart:no-watch": "pm2 restart pdms-rh --watch=false",
    "pm2:stop": "pm2 stop pdms-rh",
    "pm2:delete": "pm2 delete pdms-rh",
    "pm2:logs": "pm2 logs pdms-rh",
    "pm2:status": "pm2 status"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.1",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "nodemon": "^3.0.1",
    "sass": "^1.99.0",
    "supertest": "^7.2.2"
  }
}
